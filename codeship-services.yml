service: 
    build:
        context: .
        dockerfile: Dockerfile
        encrypted_env_file: buildargs.env.encrypted
    container_name: service
    depends_on:
        - db
        - queue
    environment:
        NODE_ENV : testing

